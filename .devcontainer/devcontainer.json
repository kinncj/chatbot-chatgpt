{
    "image":"mcr.microsoft.com/devcontainers/universal:2",
    "postCreateCommand": "python -m pip install pip-tools && pip install -r requirements.txt && set -a && source ./config/.env && set +a"
}
